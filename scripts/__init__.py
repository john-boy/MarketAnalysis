"""CLI entry points.

Scripts are invoked via ``python -m scripts.<name>`` or as
console scripts declared in ``pyproject.toml``.  They share the
application's configuration loader and data layer.
"""
